#!/bin/bash

# This script is being sourced in all other scripts that are used to compute things. Changing variables here will also change them there, so that there's a single place to do it if you need to.

BASEDIR="/data/users/$USER/RNASEQ"
echo "sourced config.sh"
